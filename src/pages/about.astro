---
import AppShell from "../layouts/AppShell.astro";
import SEO from "../components/SEO.astro";
import SystemFieldHero from "../components/personal/SystemFieldHero";
import SystemSpecCard from "../components/personal/SystemSpecCard";
import SignalGrid from "../components/personal/SignalGrid";
import WorldsDiagram from "../components/personal/WorldsDiagram";
import ExplorationQuadrants from "../components/personal/ExplorationQuadrants";
import MovementDial from "../components/personal/MovementDial";
import ProcessLanes from "../components/personal/ProcessLanes";
import SettingsBoard from "../components/personal/SettingsBoard.astro";
import AppDock from "../components/personal/AppDock.astro";
import ConsolePreview from "../components/personal/ConsolePreview.astro";
import {
  appDock,
  explorationQuadrants,
  heroBadge,
  movementDial,
  processes,
  settingsBoard,
  signalGrid,
  specSheet,
  statusTemplates,
  systemField,
  worldsDiagram,
} from "../data/personalOs";
---

<AppShell>
  <SEO
    slot="head"
    title="About · Ashton Hawkins"
    description="A personal operating system snapshot—movement, cycling, recovery, and exploration."
  />

  <main class="mx-auto flex max-w-6xl flex-col gap-14 px-4 py-12 sm:px-6 lg:px-8">
    <section class="space-y-6">
      <div class="flex flex-col gap-4">
        <p class="text-sm uppercase tracking-[0.12em] text-text-tertiary">Personal OS</p>
        <h1 class="text-balance text-4xl font-semibold text-text-primary sm:text-5xl">Ashton Hawkins</h1>
        <p class="text-lg leading-relaxed text-text-secondary sm:text-xl">
          I optimize experiences, itineraries, and systems—for screens and real life. This page is my flagship: a live-feeling operating system for how I move.
        </p>
        <div class="flex flex-wrap gap-2 text-sm text-text-secondary">
          {heroBadge.tags.map((mode) => (
            <span class="rounded-full border border-border bg-surface px-3 py-1 text-sm shadow-sm">{mode}</span>
          ))}
        </div>
        <p class="rounded-2xl border border-border/70 bg-surface/90 px-4 py-3 text-base leading-relaxed text-text-secondary">
          {statusTemplates[Math.floor(Math.random() * statusTemplates.length)]}
        </p>
      </div>

      <div class="space-y-10">
        <div class="space-y-3">
          <p class="text-xs uppercase tracking-[0.16em] text-text-tertiary">Chapter 1 · Identity & system state</p>
          <p class="text-lg text-text-secondary">
            Who I am, how I’m running, and what “Personal OS” means day to day. It starts with movement, mid-7s sleep, and a healthy curiosity loop.
          </p>
        </div>
        <SystemFieldHero
          client:load
          title={systemField.title}
          version={systemField.version}
          stability={systemField.stability}
          clusters={systemField.clusters}
        />
        <SystemSpecCard
          client:visible
          hostname={specSheet.hostname}
          os={specSheet.os}
          mode={specSheet.mode}
          loops={specSheet.loops}
          health={specSheet.health}
          modeHeat={specSheet.modeHeat}
        />
      </div>
    </section>

    <section class="space-y-8">
      <div class="space-y-2">
        <p class="text-xs uppercase tracking-[0.16em] text-text-tertiary">Chapter 2 · Worlds & signals</p>
        <p class="text-lg text-text-secondary">
          The arenas I operate in and the signals that keep them humming. Travel informs product sense, cycling steadies recovery, and all of it keeps exploration online.
        </p>
      </div>
      <WorldsDiagram client:visible worlds={worldsDiagram} />
      <SignalGrid client:visible gauges={signalGrid.gauges} heroMetrics={signalGrid.heroMetrics} />
      <MovementDial
        client:visible
        ticks={movementDial.ticks}
        filledTicks={movementDial.filledTicks}
        cyclingProgress={movementDial.cyclingProgress}
        recoveryProgress={movementDial.recoveryProgress}
        label={movementDial.label}
      />
      <ExplorationQuadrants client:visible quadrants={explorationQuadrants} />
    </section>

    <section class="space-y-8">
      <div class="space-y-2">
        <p class="text-xs uppercase tracking-[0.16em] text-text-tertiary">Chapter 3 · How I operate</p>
        <p class="text-lg text-text-secondary">
          Processes that are running, the ones warming up, and the preferences that keep things steady. From here, you can hop to Now, Portfolio, Writing, or Activity.
        </p>
      </div>
      <ProcessLanes client:visible cpuLoad={processes.cpuLoad} items={processes.items} />
      <SettingsBoard settings={settingsBoard} />
      <AppDock items={appDock} />
    </section>

    <ConsolePreview />
  </main>
</AppShell>
