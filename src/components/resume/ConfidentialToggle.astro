---
import ConfidentialToggleButton from "./ConfidentialToggleButton";
import type { ConfidentialMode } from "../../utils/confidential";
const { initialMode } = Astro.props as { initialMode: ConfidentialMode };
---
<ConfidentialToggleButton client:load initialMode={initialMode} />
