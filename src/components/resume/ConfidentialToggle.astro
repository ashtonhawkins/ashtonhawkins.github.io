---
const { isConfidential } = Astro.props as { isConfidential: boolean };
---
<button
  type="button"
  class="inline-flex items-center gap-2 rounded-[var(--r-2)] border border-[color:color-mix(in_srgb,var(--ink)_18%,transparent)] bg-[color:color-mix(in_srgb,var(--surface)_96%,transparent)] px-4 py-2 text-xs font-semibold uppercase tracking-[0.24em] text-[color:var(--ink)]"
  data-confidential-toggle
  data-state={isConfidential ? "confidential" : "public"}
>
  <span aria-hidden="true" data-icon>{isConfidential ? "🔒" : "🔓"}</span>
  <span data-label>{isConfidential ? "Confidential" : "Public"}</span>
</button>
