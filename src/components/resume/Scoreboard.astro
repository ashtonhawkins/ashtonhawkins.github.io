---
import ScoreboardClient from "./ScoreboardClient";
import type resumeData from "../../data/resume.json";
const { kpis } = Astro.props as { kpis: typeof resumeData.kpis };
---
<ScoreboardClient client:load kpis={kpis} />
