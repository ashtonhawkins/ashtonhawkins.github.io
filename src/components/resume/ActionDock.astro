---
import ActionDockButton from "./ActionDockButton";
import type resumeData from "../../data/resume.json";
const { data, bio } = Astro.props as { data: typeof resumeData; bio: string };
---
<ActionDockButton client:load data={data} bio={bio} />
